using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AggresiveBullet : MonoBehaviour {

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}

    public void Activate(GameObject target, GameObject self )
    {
        // = Vector3.MoveTowards(this.transform.position, enemy.gameObject.transform.position, .1f);

    }
}
